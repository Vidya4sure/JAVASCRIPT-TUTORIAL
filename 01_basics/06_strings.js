// [STRINGS]


// We can declare String in two ways :
// #1
const name = "vidya"
const repoCout = 50

//#2
const gameName = new String ('vidya4sure') // By Using this method you are making an oject 
let userName = new String ('hansdavsh')

console.log(userName);

console.log(name + repoCout + " Value"); // Not used in industries // Outdated syntax // (+) is also used for combine words

// Modern syntax // Backticks // String interpulation 
console.log(`Hello my name is ${name} and my repo cout is ${repoCout}`);